services:
  web:
    build: .
    image: barefoot-lens

    env_file:
      - .env

    environment:
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production

    ports:
      - "127.0.0.1:${PORT:-3000}:${PORT:-3000}"

    restart: unless-stopped
