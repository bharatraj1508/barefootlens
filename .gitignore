# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
**/.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/

# docker
# (Dockerfile & compose are tracked; ignore local overrides)
docker-compose.override.yml

# raw source assets — large master files not needed to build/run the site
# (the app serves everything from /public). Keep your own backup of this.
/data
